version: 3
projects:
  - name: staging
    dir: environment/staging
    workspace: default
    autoplan:
      when_modified: ["*.tf", "*.tfvars"]
      enabled: true
    branches:
      - dev
  - name: production
    dir: environment/production
    workspace: default
    autoplan:
      when_modified: ["*.tf", "*.tfvars"]
      enabled: true
    branches:
      - main
      - dev
